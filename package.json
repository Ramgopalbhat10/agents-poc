{
  "name": "agents-poc",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bash -lc \"bun --cwd apps/api run dev & bun --cwd apps/agents run dev & bun --cwd apps/web run dev & wait\"",
    "dev:api": "bun run --cwd apps/api dev",
    "dev:agents": "bun run --cwd apps/agents dev",
    "dev:web": "bun run --cwd apps/web dev"
  }
}
